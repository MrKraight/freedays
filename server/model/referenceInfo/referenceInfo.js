class ReferenceInfo{
    constructor(describeInfo, pathInfo, planInfo) 
    {
        this.describeInfo = describeInfo,
        this.pathInfo = pathInfo,
        this.planInfo = planInfo
    }
}

module.exports = ReferenceInfo;